---
interface Props {

  title: string;
  description: string;
  link?: string;
  github?: string;
  image: string;
  tags: string;

}

const { title, description, link, github, image,  tags} = Astro.props;
---
